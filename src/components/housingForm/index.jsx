import React from 'react'
import { kasaList } from '../../kasaList'
import SlideShow from '../slideShow'

const HousingForm = (props) => {
  return (
    <div>
      <SlideShow id={props.id} />
    </div>
  )
}

export default HousingForm
